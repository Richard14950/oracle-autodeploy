# Déploiement automatique du proxy Saxo (Oracle Cloud)

Ce dossier contient tout le nécessaire pour déployer automatiquement un proxy Saxo LIVE sur une VM Ubuntu Oracle Cloud.

---

## 1. Préparation de la VM Oracle

Créer une VM Ubuntu (shape Always Free) puis :

```bash
sudo apt update -y
sudo apt install -y git

